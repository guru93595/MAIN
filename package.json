{
    "name": "evara-monorepo",
    "private": true,
    "scripts": {
        "install:client": "cd client && npm install",
        "install:server": "cd server && pip install -r requirements.txt",
        "setup": "npm run install:client && npm run install:server",
        "dev:client": "cd client && npm run dev",
        "dev:server": "cd server && set PYTHONIOENCODING=utf-8 && python -m uvicorn main:app --reload --port 8000",
        "start": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
        "build": "cd client && npm install && npm run build"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "supabase": "^2.76.15"
    }
}
